# Verify

Prove that the current implementation works. Don't just say it works — demonstrate it.

Branch vs main diff:
```
!`git diff main...HEAD --stat`
```

Instructions:
1. Identify what changed (from the diff above).
2. Run the test suite. Show the output.
3. If there are no tests, run the CLI with representative inputs and show actual output.
4. Diff behavior between `main` and the current branch if relevant.
5. Report: what was verified, what passed, what still needs attention.
6. If something fails, fix it — don't just report it.

$ARGUMENTS
